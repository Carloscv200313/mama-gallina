import Link from "next/link";
import { ArrowLeft, Utensils } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { requireRole } from "@/lib/auth/server";
import { formatCurrency, orderStatusClass, orderStatusLabel } from "@/lib/pos/types";
import { createAdminClient } from "@/lib/supabase/admin";
import { OrderDetailActions } from "@/components/orders/order-detail-actions";

type Item = { id: string; product_name_snapshot: string; variant_name_snapshot: string | null; quantity: number; unit_price: number; modifiers_total: number; line_total: number; notes: string | null; priority: string; status: string };

export default async function OrderDetailPage({ params }: { params: Promise<{ id: string }> }) {
  const context = await requireRole("admin", "waiter", "cashier", "kitchen");
  const { id } = await params;
  const admin = createAdminClient();
  const [{ data: order }, { data: itemData }] = await Promise.all([admin.from("orders").select("id, order_code, status, total, subtotal, balance_due, payment_total, order_type, table_id, waiter_id, customer_name, notes, opened_at, sent_to_kitchen_at").eq("id", id).eq("branch_id", context.profile.branchId).maybeSingle(), admin.from("order_items").select("id, product_name_snapshot, variant_name_snapshot, quantity, unit_price, modifiers_total, line_total, notes, priority, status").eq("order_id", id).order("created_at")]);
  if (!order) return <div className="mx-auto max-w-3xl"><Card><CardContent className="py-16 text-center"><p className="font-semibold">Pedido no encontrado</p><Button asChild variant="outline" className="mt-5"><Link href="/pedidos">Volver a pedidos</Link></Button></CardContent></Card></div>;
  const items = (itemData ?? []) as Item[];
  const { data: modifierData } = items.length ? await admin.from("order_item_modifiers").select("order_item_id, option_name_snapshot, additional_price, quantity").in("order_item_id", items.map((item) => item.id)) : { data: [] };
  const modifiersByItem = new Map<string, Array<{ option_name_snapshot: string; additional_price: number; quantity: number }>>();
  for (const modifier of (modifierData ?? []) as Array<{ order_item_id: string; option_name_snapshot: string; additional_price: number; quantity: number }>) modifiersByItem.set(modifier.order_item_id, [...(modifiersByItem.get(modifier.order_item_id) ?? []), modifier]);
  return <div className="mx-auto max-w-5xl space-y-6"><div className="flex items-start gap-3"><Button asChild variant="ghost" size="icon"><Link href="/pedidos"><ArrowLeft className="size-5" /></Link></Button><div><p className="text-sm font-semibold uppercase tracking-[0.16em] text-brand-olive">Detalle de pedido</p><h1 className="mt-1 font-display text-4xl font-bold">{order.order_code}</h1><p className="mt-2 text-sm text-brand-olive">{order.order_type === "takeaway" ? "Para llevar" : "Pedido en mesa"} · {new Date(order.opened_at).toLocaleString("es-PE", { dateStyle: "medium", timeStyle: "short" })}</p></div><Badge className={`ml-auto ${orderStatusClass(order.status)}`}>{orderStatusLabel(order.status)}</Badge></div><div className="grid gap-5 lg:grid-cols-[1fr_320px]"><Card><CardHeader><CardTitle className="flex items-center gap-2"><Utensils className="size-5" /> Productos</CardTitle></CardHeader><CardContent className="space-y-3">{items.map((item) => <div key={item.id} className="rounded-xl border border-brand-olive/10 p-4"><div className="flex items-start justify-between gap-3"><div><p className="font-semibold">{item.quantity} × {item.product_name_snapshot}</p>{item.variant_name_snapshot ? <p className="text-sm text-brand-olive">{item.variant_name_snapshot}</p> : null}{modifiersByItem.get(item.id)?.length ? <p className="mt-1 text-xs text-brand-olive">{modifiersByItem.get(item.id)?.map((modifier) => `${modifier.option_name_snapshot}${modifier.quantity > 1 ? ` × ${modifier.quantity}` : ""}`).join(" · ")}</p> : null}{item.notes ? <p className="mt-2 text-xs italic text-brand-brown">Nota: {item.notes}</p> : null}</div><div className="text-right"><Badge variant="muted">{item.status === "pending" ? "Pendiente" : item.status === "preparing" ? "En preparación" : item.status === "ready" ? "Listo" : item.status === "delivered" ? "Entregado" : "Anulado"}</Badge><p className="mt-2 font-semibold">{formatCurrency(Number(item.line_total))}</p></div></div></div>)}</CardContent></Card><div className="space-y-5"><Card><CardHeader><CardTitle>Resumen de pago</CardTitle></CardHeader><CardContent className="space-y-3 text-sm"><div className="flex justify-between"><span className="text-brand-olive">Subtotal</span><span>{formatCurrency(Number(order.subtotal))}</span></div><div className="flex justify-between border-t border-brand-olive/10 pt-3 text-lg font-bold"><span>Total</span><span>{formatCurrency(Number(order.total))}</span></div><div className="flex justify-between"><span className="text-brand-olive">Pagado</span><span>{formatCurrency(Number(order.payment_total))}</span></div><div className="flex justify-between font-semibold text-red-700"><span>Saldo</span><span>{formatCurrency(Number(order.balance_due))}</span></div></CardContent></Card><OrderDetailActions orderId={id} status={order.status} roles={context.roles} canEdit={(context.roles.includes("admin") || context.roles.includes("waiter")) && (order.waiter_id === context.staffId || context.roles.includes("admin")) && !["payment_pending", "paid", "cancelled"].includes(order.status)} /></div></div></div>;
}
